import type { Settings } from '@/types';

export const mockSettings: Settings = {
  theme: 'dark',
  fontSize: 14,
  tabSize: 4,
  autoSave: true,
  telemetryEnabled: true,
  checkpointInterval: 5000,
  defaultRunner: 'fozzy-native',
};
